package agent

import "github.com/volcano-sh/volens/internal/agent/model"

type Request = model.Request

type Check = model.Check

type NodeResult = model.NodeResult

type Outcome = model.Outcome

type StageState = model.StageState

type ResourcePair = model.ResourcePair

type QueueResourceValue = model.QueueResourceValue

type QueueSummary = model.QueueSummary

type PodGroupSummary = model.PodGroupSummary

type EnqueueReport = model.EnqueueReport

type WorkloadRow = model.WorkloadRow

type JobValidReport = model.JobValidReport

type AllocateReport = model.AllocateReport

type Diagnosis = model.Diagnosis

type SchedulerPolicy = model.SchedulerPolicy

type ConfiguredPlugin = model.ConfiguredPlugin

type PluginTier = model.PluginTier

type PluginHookReport = model.PluginHookReport

type ActionReport = model.ActionReport

type Report = model.Report
